import { MOCK_COUNTRY } from "@/models/__tests__/__mocks__/Country.mock";
import type { INetwork } from "@/models/Network";

export const MOCK_NETWORK: INetwork = {
  id: 39,
  name: "ESPN",
  country: MOCK_COUNTRY,
};
